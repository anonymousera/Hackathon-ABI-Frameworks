# Copy this file to .env and fill in your key to enable the LLM extraction fallback.
# Without these, the pipeline runs rules-only (no LLM calls).

OPENAI_API_KEY=sk-your-key-here

# Optional: override the default model (gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
